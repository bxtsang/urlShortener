micronaut:
  application:
    name: urlShortener

  server:
    cors:
      enabled: true
  router:
    static-resources:
      default:
        enabled: true
        mapping: "/home/**"
        paths: classpath:public

domain:
  location: http://localhost:8080/url
